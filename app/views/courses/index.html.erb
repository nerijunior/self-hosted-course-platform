<p style="color: green"><%= notice %></p>

<% content_for :title, "Todos os cursos" %>

<div id="courses" class="mb-3 grid grid-cols-3 gap-4">
  <% @courses.each do |course| %>
    <%= render course %>
  <% end %>

  <% if @courses.empty? %>
    Nenhum curso carregado
  <% end %>
</div>
